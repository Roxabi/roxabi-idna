[Unit]
Description=roxabi-idna picker server (port 8082)
After=network.target

[Service]
Type=simple
WorkingDirectory=%h/projects/roxabi-idna
ExecStart=/bin/bash -lc 'uv run idna_server.py'
Restart=on-failure
RestartSec=5
Environment=HOME=%h

[Install]
WantedBy=default.target